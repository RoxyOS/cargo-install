[package]
name = "cargo-install"
version = "0.1.0"
edition = "2024"
description = "Wrapper around the cargo install command"
readme = "README.md"

[dependencies]
derive_builder = "0.20"
tap = "1"
thiserror = "2"

[dev-dependencies]
tempfile = "3"
